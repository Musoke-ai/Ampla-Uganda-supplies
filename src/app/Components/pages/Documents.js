import DocumentsPage from "../../documents/documents";
import { useState } from "react";
import Invoice from '../../documents/Invoice';

const Documents = () => {
   
return (
    <div>
      <Invoice />
      {/* <InvoicesManager /> */}
        {/* <DocumentsPage /> */}

        {/* <div>
      <button onClick={triggerSuccess}>Trigger Success</button>
      <button onClick={triggerError}>Trigger Error</button>
      <PopupMessage
        visible={popup.visible}
        type={popup.type}
        message={popup.message}
        onClose={() => setPopup({ visible: false })}
      />
    </div> */}
 {/* <Notification notifications={notifications} /> */}
 {/* <DebtPage /> */}
 {/* <ReportsPage /> */}
 {/* <button onClick={() => triggerNotification(true)}>Show Success</button>
      <button onClick={() => triggerNotification(false)}>Show Error</button>

      <Notification
        isOpen={isOpen}
        isSuccess={notificationType}
        message={message}
        duration={5000} // 5 seconds
        onClose={() => setIsOpen(false)}
      /> */}
      {/* <NavbarSample /> */}
    </div>
)
}

export default Documents;